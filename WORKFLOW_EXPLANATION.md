# 📋 شرح فكرة النظام وتصميمه

## 🎯 الفكرة الأساسية

النظام يعمل على مبدأ **"نص واحد → زيارة كاملة"**:
- الطبيب يكتب نص واحد في حقل نصي
- النظام يقسم النص تلقائياً ويحفظه في الجداول المناسبة
- كل نص يصبح **زيارة (Visit)** كاملة بتاريخ اليوم

---

## 🔄 سير العمل (Workflow)

### 1️⃣ **الطبيب يفتح موعد (Appointment)**
   - يذهب إلى `/admin/appointments/{id}/edit`
   - يجد حقل نصي كبير في أسفل الصفحة (فقط للطبيب)

### 2️⃣ **الطبيب يكتب النص**
   ```
   Medications:
   Metformin 500mg twice daily
   Lisinopril 10mg once daily

   Diseases:
   Diabetes Type 2
   Hypertension

   Habits:
   Smoking: non-smoker
   Caffeine: 2-3 cups per day

   Symptoms:
   Headache, fatigue

   Notes:
   Patient responding well to treatment
   ```

### 3️⃣ **عند الحفظ (Save)**
   - ✅ يتم إنشاء **Visit** بتاريخ اليوم
   - ✅ يتم حفظ الأدوية في `patient_medications`
   - ✅ يتم حفظ الأمراض في `patient_diseases`
   - ✅ يتم تحديث العادات في `patient_habits`
   - ✅ كل البيانات مرتبطة بتاريخ الزيارة

### 4️⃣ **المريض يرى البيانات**
   - في صفحة الموعد، يجد **6 تبويبات (Tabs)**:
     1. **Patient Information** - معلومات المريض
     2. **Patient Visits** - جميع الزيارات (مع زر PDF لكل زيارة)
     3. **Patient Medications** - جميع الأدوية
     4. **Patient Diseases** - جميع الأمراض
     5. **Patient Habits** - العادات
     6. **Patient Attachments** - المرفقات

### 5️⃣ **طباعة زيارة معينة**
   - المريض يضغط على زر "Download PDF" بجانب أي زيارة
   - يتم تنزيل PDF يحتوي على:
     - معلومات المريض والطبيب
     - تفاصيل الزيارة (تاريخ، أعراض، تشخيص)
     - الأدوية الموصوفة في هذه الزيارة
     - الأمراض المشخصة في هذه الزيارة

---

## 🗂️ البنية التقنية

### **Models (النماذج)**
```
User (Patient)
├── hasMany PatientMedication
├── hasMany PatientDisease
├── hasOne PatientHabit
├── hasMany PatientAttachment
└── hasMany Visit

Visit
├── belongsTo User (patient)
├── belongsTo Doctor
└── visit_date (تاريخ الزيارة)

Appointment
├── belongsTo User (patient)
└── belongsTo Doctor
```

### **Services (الخدمات)**
- **MedicalTextParserService**: يقسم النص ويحفظه في الجداول
  - يكتشف الأقسام (Medications, Diseases, Habits, Symptoms, Notes)
  - يحلل كل سطر ويستخرج البيانات
  - ينشئ Visit أولاً ثم يربط البيانات بها

### **Filament Resources**
- **AppointmentResource**: المواعيد
  - **RelationManagers**: 6 جداول لعرض بيانات المريض
  - **EditAppointment Page**: صفحة التعديل مع حقل النص

---

## 📊 مثال على البيانات

### **قبل كتابة النص:**
- Visit: ❌ لا يوجد
- Medications: 0
- Diseases: 0

### **بعد كتابة النص وحفظه:**
- Visit: ✅ تم إنشاؤها بتاريخ 2025-11-25
- Medications: 2 (Metformin, Lisinopril)
- Diseases: 2 (Diabetes, Hypertension)
- Habits: ✅ تم التحديث

---

## 🎨 التصميم

### **صفحة تعديل الموعد:**
```
┌─────────────────────────────────────┐
│  Appointment Details                │
│  - Patient Name                     │
│  - Doctor Name                      │
│  - Appointment Time                 │
│  - Status                           │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  Quick Medical Entry (Creates Visit)│
│  ┌───────────────────────────────┐ │
│  │  Medical Text (Textarea)       │ │
│  │  [Write here...]              │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘

[Tabs]
├── Patient Information
├── Patient Visits (مع زر PDF لكل زيارة)
├── Patient Medications
├── Patient Diseases
├── Patient Habits
└── Patient Attachments
```

---

## 🔑 النقاط المهمة

1. **كل نص = زيارة واحدة**: كل مرة يكتب الطبيب نص ويحفظه، يتم إنشاء زيارة جديدة
2. **التاريخ تلقائي**: `visit_date = now()` - تاريخ اليوم
3. **البيانات مرتبطة**: الأدوية والأمراض مرتبطة بتاريخ الزيارة
4. **PDF لكل زيارة**: المريض يطبع زيارة معينة فقط
5. **عرض شامل**: كل بيانات المريض في مكان واحد (6 تبويبات)

---

## 💡 الفائدة

- **للطبيب**: يكتب نص واحد بدلاً من ملء 5 نماذج منفصلة
- **للمريض**: يرى كل بياناته في مكان واحد ويمكنه طباعة أي زيارة
- **للنظام**: بيانات منظمة ومرتبطة ببعضها






